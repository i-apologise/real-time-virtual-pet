extends Node
## Stub — global signals for pet/profile/UI updates (expanded in later PRs).

signal pet_updated(snapshot: Dictionary)
signal profile_updated(snapshot: Dictionary)
signal status_message(text: String)
